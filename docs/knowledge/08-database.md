# 数据库相关

> 待 Plan 1 / Task 13、Task 17 引入。

将涉及的概念预告：
- PostgreSQL 基础
- 关系型数据库的事务（ACID）
- 连接池（connection pool）
- ORM（GORM）
- 数据库迁移工具（golang-migrate）—— up / down / 版本号
- JSONB 字段
- 索引与查询性能
- `SELECT ... FOR UPDATE SKIP LOCKED`（多 Worker 并发安全取任务）
- Redis 数据结构（String / List / Hash / Set / Stream / Pub-Sub）
- TTL 与 LRU 淘汰策略
